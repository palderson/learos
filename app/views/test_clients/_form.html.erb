<%= simple_form_for(project.with_blank_fields, html: { class: 'form-horizontal active-table-form' }) do |f| %>
  <%= f.simple_fields_for :test_clients do |nf| %>
    <%= nf.input :description %>
  <% end %>

  <div class='form-actions'>
    <%= f.submit 'Save', class: 'btn btn-primary' %>
  </div>
<% end %>

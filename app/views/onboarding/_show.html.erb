<%= project.onboarding.step_one %>
<%= project.onboarding.step_two %>
<%= project.onboarding.step_three %>
<%= project.onboarding.step_four %>
<%= project.onboarding.step_five %>

<% if can? :edit, @project %>
  <%= link_to 'Edit', edit_project_path(@project) %>
<% end %>

<%= render partial: 'comments/form', locals: { project: @project, nested: :onboarding } %>
